import { UserManualScreen } from '@components/screens';
import PrivacyPolicyScreen from '@components/screens/privacy-policy';
import { LoginScreen } from '@features/auth/screens';

export default function page() {
  return <UserManualScreen />;
}
